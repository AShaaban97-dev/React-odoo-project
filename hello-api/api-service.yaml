apiVersion: v1
kind: Service

metadata:
  name: hello-api
  namespace: hello-api

spec:
  selector:
    app: hello-api

  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30001

  type: NodePort
